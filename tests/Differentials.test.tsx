import React from "react";
import { render } from "@testing-library/react-native";
import {Diefferentials } from "../components";

describe("<Diefferentials/>", () => {
	it("it can render differentials sections", () => {
		render(<Diefferentials />);
	});
});
